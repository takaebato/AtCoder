#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep1(i, n) for (int i = 1; i < (int)(n); i++)

int main() {
    int N, M;
    cin >> N >> M;
    vector<int> A(N + 1);
    vector<int> C(N + M + 1);
    rep(i, N + 1) cin >> A[i];
    rep(i, M + N + 1) cin >> C[i];
    vector<int> B(M + 1);

    rep()
}
