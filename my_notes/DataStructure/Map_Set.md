# Map / Set

http://www.tommylab.ynu.ac.jp/lecture/Algorithm/14/14.pdf

- set
  - 要素アクセス
```cpp
set<int> s;
first = *s.begin();
s.erase(s.begin());
```

- find
```cpp
if(mp.find(key)==mp.end()) // not found
```